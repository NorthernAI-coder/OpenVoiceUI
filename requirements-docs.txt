# Optional: document text extraction for /api/upload
# Install this to enable PDF/DOCX/XLSX/PPTX content preview in the transcript panel.
# Without it, those file types still upload successfully — the AI just can't read their contents.
#
# Install: pip install -r requirements-docs.txt
pypdf>=6.11.0
python-docx>=1.2.0
openpyxl>=3.1.5
python-pptx>=1.0.2
